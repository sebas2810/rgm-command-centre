services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: rgm_command_centre
      POSTGRES_USER: rgm
      POSTGRES_PASSWORD: rgm_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
